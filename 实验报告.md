# 《一箭又一箭》小游戏 · 实验报告

| 项目 | 内容 |
| --- | --- |
| 作业 | 借助 AIGC 工具独立开发一个「一箭又一箭」风格的小游戏 |
| 开发语言 / 库 | Python 3.12 + Pygame 2.6.1（无任何图片 / 音频素材文件） |
| 代码规模 | 18 个 Python 文件，约 2900 行（逻辑层 / 界面层 / 测试 / 工具） |
| 关卡 | 30 关，全部程序化随机生成且保证可解 |
| 测试 | 38 个自动化用例（规则 21 + 界面 11 + 作业测试表 T01–T06） |
| 运行方式 | `pip install pygame` 后 `python arrow_arrows/main.py` |

---

## 一、需求分析

原游戏的核心循环是一句话：**棋盘上摆满箭头，玩家按合适的顺序点击，让所有箭头依次飞出棋盘**。
把它拆成可实现的需求：

| 编号 | 需求 | 对应实现 |
| --- | --- | --- |
| R1 | 二维棋盘与上下左右四方向箭头的数据表示 | `core/model.py`：`Board` + `Arrow` + `Direction` |
| R2 | 方向判定：箭头只能朝它指的方向飞 | `Direction.dx/dy`、`Board.ray_cells` |
| R3 | 路径检测：头前方到棋盘边缘必须整条线为空 | `Board.scan` / `Board.is_free`（位掩码 O(1)） |
| R4 | 碰撞反馈：点中被挡住的箭头要有明显反馈 | `Board.click` 返回 `ClickResult`，界面播放抖动、闪红、红圈、飘字、音效 |
| R5 | 失误次数：设置、显示、耗尽即失败 | `App.lives` + HUD「剩余机会 ♥♥♥」+ 失败弹层 |
| R6 | 关卡必须能通关，且至少 3 关 | `core/generator.py` 逆向走廊构造法 + 求解器自检；共 30 关 |
| R7 | 关卡状态管理：通关 / 失败 / 重开 / 下一关 | `ui/app.py` 状态机（menu / levels / help / game + paused / win / lose 弹层） |
| R8 | 图形化界面（允许 Pygame / Pygame Zero / Tkinter） | 选 Pygame：动画、半透明、等比缩放都更好做 |
| R9 | 借助 AIGC 完成，并说明对 AI 代码的判断、修改与完善 | 见第五节 |

三条贯穿全程的设计原则：

1. **规则层与界面层彻底分开**：`core/` 不 import pygame，可脱开界面单独跑单元测试；界面层只调用规则接口。
2. **玩法上保证不会卡死**：移除一个箭头只会让路径更空，自由箭头集合单调递增，
   玩家一直点能飞的箭头必定通关；难度来自「在几十个箭头里找出能点的那几个」和失误惩罚。
3. **零素材依赖**：图形全部用 Pygame 绘制指令生成，音效用标准库 `array` 合成 PCM，
   只要装了 pygame 就能跑，不会出现资源缺失。

---

## 二、系统设计

### 2.1 分层架构

```
        ┌──────────────────────────── ui/ 界面层 ────────────────────────────┐
        │ app.py         主循环 / 状态机 / 输入 / 关卡流程 / 结算与存档        │
        │ board_view.py  坐标换算 + 棋盘绘制 + 动画(飞出·抖动·飘字·提示)      │
        │ scenes.py      主菜单·选关·说明·HUD·暂停·过关·失败                  │
        │ widgets / shapes / theme / audio / storage：按钮·图形·配色·音效·存档│
        └───────────────────────────────┬─────────────────────────────────────┘
                                        │ 只调用公开方法，不自己判规则
        ┌───────────────────────────────▼─────────────────────────────────────┐
        │ core/ 逻辑层（不依赖 pygame，可单独测试）                            │
        │ model.py     Board / Arrow / Direction / Hit / ClickResult           │
        │ generator.py 难度曲线 + 逆向走廊构造法（保证可解）                   │
        │ solver.py    贪心求解（自检）、H 键提示                              │
        └─────────────────────────────────────────────────────────────────────┘
```

一次点击的数据流：

```
鼠标点击 → App.to_logical 换算回逻辑坐标 → BoardView.px_to_cell 得到格子
        → Board.arrow_at 取箭头 → Board.click 判定
             ├─ 能飞：Board.remove_arrow + BoardView.spawn_flying + 音效
             │        → 棋盘清空则 App.finish_win → 星级 / 存档
             └─ 被挡：BoardView.spawn_blocked（抖动 + 红框 + 飘字 + 圈出拦路箭头）
                      → lives -= 1 → 归零则显示失败弹层
```

### 2.2 关键数据结构

```python
@dataclass
class Arrow:
    id: int
    x: int; y: int          # 所在格子，(0,0) 在左上角，x 向右、y 向下
    direction: Direction    # UP / RIGHT / DOWN / LEFT，value 即 (dx, dy)
    color: int              # 调色板下标，界面层解释成霓虹色
```

* 一个箭头占一格、带一个方向，规则因此非常干净：**能不能飞 = 它正前方那条射线是否畅通**，
  不需要做矩形碰撞，也不需要区分「箭头身体」和「箭头头」。
* 棋盘为每一行、每一列各维护「箭头 / 墙」两个位掩码，一次位运算拿到最近障碍物：

```python
am = self._arrow_row[y] >> (x + 1)      # 前方所有箭头压缩成一个整数
wm = self._wall_row[y]  >> (x + 1)
da = (am & -am).bit_length() - 1        # 最近箭头距离（取最低位）
dw = (wm & -wm).bit_length() - 1        # 最近墙距离
# 两个掩码都是 0 → 一路畅通，判定 "edge"，可以飞
```

* 界面渲染时按格子中心画一枚霓虹箭头（圆头短杆 + 三角头 + 柔光 + 深色描边），
  同一条走廊上的箭头颜色各不相同，看起来就是一串彩色的小箭头（见截图）。
* 说明：开发中曾尝试过「一枚箭头占 1~4 格、连成彩色长条」的方案，实现完成并修好一个
  会导致关卡不可解的判据 Bug 之后，实际渲染出来的棋盘偏拥挤杂乱，最终判断**收益不大、复杂度更高**，
  回退成单格箭头（详见 5.2 案例 5）。

### 2.3 状态机

`menu → levels / help / game`，`game` 之上可叠加 `paused / win / lose` 三种弹层。
每个场景的 `draw_*` 函数会**返回本帧的按钮列表**，`App` 直接拿它做命中判定——
绘制与可点击区域永远是同一份数据，不会出现「按钮画在这里、能点的区域在那里」。

---

## 三、关键算法

### 3.1 关卡生成：逆向走廊构造法

**（1）可解性证明。** 设正确顺序为 `r1…rn`（`r1` 最先点）。点到 `ri` 时场上还剩 `ri…rn`，
故 `ri` 头前方射线上不能有**更晚被点掉**的箭头；更早被点掉的 `r1…r(i-1)` 在它射线上无所谓。
因此只要构造时满足 **「每放一个新箭头，它头前方的射线没有已放箭头、也没有墙」**，
则「按放置顺序倒着点」必为合法解：

* 合法性：旧箭头不会落在新箭头的头前方射线上 → 倒序点击时先飞走的新箭头不会挡着旧箭头；
* 可解性：新箭头放下时前方射线为空，之后只有更晚放的箭头可能进入它的射线，
  而这些箭头倒序里排在它前面先飞走，轮到它时射线必然又空。

**（2）走廊策略。** 只满足约束还不够——随机撒点会放出大量「随时能点」的自由箭头。
于是改成一条条铺走廊：找一条最长的可用射线当起点 → 沿该方向放一个箭头 → 紧接着在它正前方
那一格起下一个箭头，65% 概率拐 90°、其余直行 → 重复到铺不动。
同一条走廊上每个箭头都被紧挨着的下一个挡住，只有走廊末端能直接飞出去，
因此**可点箭头数 ≈ 走廊条数**，走廊越长越难。

**（3）难度曲线。** 由「棋盘边长、墙数量、目标箭头数、走廊上限」四个参数控制：

| 关卡 | 1 | 3 | 5 | 10 | 15 | 20 | 30 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 边长 | 4 | 5 | 6 | 8 | 10 | 10 | 10 |
| 墙 | 0 | 0 | 1 | 4 | 8 | 8 | 8 |
| 目标箭头 | 5 | 9 | 13 | 23 | 32 | 42 | 44 |
| 走廊上限 | 3 | 4 | 6 | 9 | 12 | 15 | 20 |

### 3.2 求解器与提示

```python
def greedy_solve(board):     # 每次点掉「能解锁最多箭头」的自由箭头
    while 场上有箭头:
        frees = 自由箭头集合
        if not frees: return None          # 说明关卡非法（例如有箭头被墙永久堵死）
        点掉 max(frees, key=直接挡住别人最多的那个)
```

三个用途：① 生成后自检，不通过就重生成；② 单元测试里对大量随机关卡断言「一定可解」；
③ 游戏内 `H` 键提示、`tools/verify_levels.py` 自动试玩。因为自由箭头单调递增，这个贪心不会走死。

### 3.3 动画与表现

| 效果 | 实现 |
| --- | --- |
| 箭头飞出 | 移除模型的同一帧启动动画，整条箭头按 `p^1.7` 加速平移出棋盘，另画 3 个渐淡残影做拖尾 |
| 撞墙 / 被挡 | 棋盘整体按 sin/cos 抖动 0.36 s，屏幕边缘红框闪，弹道空格闪红点，拦路箭头加红色脉冲圈 |
| 悬停预览 | 绿色虚线＝能飞（末端画出口圆点）；红色虚线 + 红圈＝被谁挡住 |
| 提示 | 链尾箭头外画两圈金色脉冲环 |
| 飘字 | 上浮 + 透明度衰减 |
| 音效 | `array` 合成 16 位 PCM 交给 `pygame.mixer.Sound`，初始化失败自动静音 |

---

## 四、测试与结果

### 4.1 自动化测试（38 个用例，约 1.2 秒跑完）

```bash
python -m unittest discover -s arrow_arrows/tests -t .
```

| 测试文件 | 用例数 | 覆盖点 |
| --- | --- | --- |
| `test_core.py` | 21 | 四方向射线判定、墙阻挡、最近阻挡者、点击结果、射线格与飞出步数、反向/垂直箭头不受影响、掩码增删一致性、克隆独立；多关卡 × 多随机种子断言**一定可解**、棋盘合法（不重叠、不与墙重合）、难度单调、同种子可复现 |
| `test_ui_smoke.py` | 11 | 各场景渲染、**自动把一关打通并拿三星**、挡路扣血与反馈动画、生命耗尽进失败弹层、提示指向可点箭头、重开保持布局 / 换局换布局、所有按钮 key 可达、窗口缩放坐标反查、最大棋盘完整落在面板内 |
| `test_assignment_cases.py` | 6 | 作业测试表 T01–T06，逐条打印「实际结果」 |

其中「单调性」用例是设计原则的自动化保障：随机点掉一个自由箭头后，
断言原本能飞的箭头仍然能飞——即玩家不可能把自己玩死。

### 4.2 作业测试表 T01–T06 实测结果

| 编号 | 测试内容 | 预期结果 | 实际结果 | 是否通过 |
| --- | --- | --- | --- | --- |
| T01 | 点击前方无阻挡的箭头 | 箭头飞出棋盘并消失 | 箭头数 13 → 12，该格已空，飞出动画 1 个 | 通过 |
| T02 | 点击前方有阻挡的箭头 | 箭头不消失，失误次数减 1 | 箭头数仍为 15，剩余机会 2/3，抖动 0.36 s，飘字 1 条 | 通过 |
| T03 | 点击位于边缘且朝向棋盘外的箭头 | 箭头正常消失，不发生越界错误 | 4 个贴边朝外箭头全部飞出，棋盘清空，弹层 win | 通过 |
| T04 | 消除本关全部箭头 | 显示通关并进入下一关 | 11 步清空、失误 0、星级 3；点「下一关」进入第 5 关 | 通过 |
| T05 | 失误次数耗尽 | 显示失败并允许重新开始 | 3 次失误后弹层 lose；「再来一次」后机会恢复 3/3 | 通过 |
| T06 | 游戏进行中重新开始 | 箭头布局和失误次数恢复 | 飞 1 个 + 撞 1 次后布局回到初始状态，机会 3/3、失误归零 | 通过 |

### 4.3 逐关试玩验证

```bash
python arrow_arrows/tools/verify_levels.py
```

用与玩家完全相同的点击链路（`App.click_board`）把 30 关自动打通一遍：

| 关卡 | 1 | 5 | 10 | 15 | 20 | 25 | 30 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 棋盘 | 4×4 | 6×6 | 8×8 | 10×10 | 10×10 | 10×10 | 10×10 |
| 箭头数 | 5 | 13 | 23 | 32 | 42 | 44 | 44 |
| 点击步数 | 5 | 13 | 23 | 32 | 42 | 44 | 44 |
| 失误 | 0 | 0 | 0 | 0 | 0 | 0 | 0 |
| 结果 | 通关 | 通关 | 通关 | 通关 | 通关 | 通关 | 通关 |

**30 关全部正常通关、零失误**，说明每关都存在合理通关顺序。

### 4.4 生成质量实测（每关 5 次生成取平均）

| 关卡 | 4 | 8 | 10 | 18 | 30 |
| --- | --- | --- | --- | --- | --- |
| 实际箭头数 | 11.0 | 19.0 | 23.0 | 38.0 | 44.0 |
| 开局可点箭头 | 2.8 | 2.8 | 3.0 | 2.6 | 3.4 |
| 可点占比 | 25.5% | 14.7% | 13.0% | 6.8% | 7.7% |
| 占格率 | 44.0% | 38.8% | 35.9% | 38.0% | 44.0% |
| 单次生成耗时 | 0.5 ms | 1.8 ms | 1.6 ms | 2.9 ms | 7.6 ms |

### 4.5 界面渲染自检

`python arrow_arrows/tools/screenshot.py` 用 SDL dummy 驱动无头渲染 12 张界面图
（主菜单、选关、说明、游戏、悬停可飞、悬停被挡、碰撞反馈、飞出瞬间、过关、失败、暂停、第 30 关），
输出在 `arrow_arrows/tools/shots/`。开发中就是靠它逐帧检查排版，发现并修掉了
「悬停提示被按钮压住」「墙和箭头格子长得一样」「提示符号显示成方块」等问题。

---

## 五、AIGC 使用记录

### 5.1 使用方式

| 环节 | 我做了什么 | AIGC 工具做了什么 |
| --- | --- | --- |
| 需求拆解 | 对照作业要求提炼 R1–R9 与三条设计原则 | 把「点击顺序、路径检测、碰撞反馈、关卡状态管理」翻译成模块清单 |
| 架构 | 定下 `core` / `ui` 分层、位掩码方案、状态机 | 生成第一版文件骨架与大部分样板代码（绘制、控件、存档） |
| 算法 | 提出「逆向构造保证可解」，要求改成走廊式生成 | 给出构造法实现，但判据有错（见 5.2 案例 1、2、3） |
| 调参 | 写诊断脚本、量化指标（占格率 / 可点箭头占比 / 耗时） | 无——三个生成器缺陷都是我跑数据发现的 |
| 外观 | 试过多格长箭头方案后判断不划算并回退；此后重新设计整套视觉（渐变柔光底、霓虹箭头、胶囊信息条、面板内嵌进度条、界面淡入） | 给出两版绘制代码与配色建议 |
| 测试 | 定测试点（可解性、单调性、自动通关、T01–T06） | 生成测试用例模板 |
| 文档 | 校对、补充实测数据 | 起草 README / 报告结构 |

### 5.2 对 AI 代码的判断、修改与完善（六个真实案例）

**案例 1：打分函数把「已经被挡住的箭头」也算成收益，后期关卡形同虚设**

AI 第一版生成器是「随机撒点 + 打分」，打分核心是「放这里能挡住几个箭头」：

```python
# AI 的原始实现
hit = board.scan(arrow.x, arrow.y, arrow.direction)
if hit.distance > dist:      # 只要第一个障碍比这个格子远，就认为“挡住了它”
    count += 1
```

诊断脚本显示第 30 关开局有 **23 个可点箭头（占 42%）**，谜题几乎没有难度。摊开某一步看：

```
准备放置: (4, 2) Direction.DOWN score = 1
  arrow Arrow#2(5,2,LEFT) dist_to_cell=1 hit=arrow at (1,2) distance=4 free=False
```

`Arrow#2` 此刻已被 `(1,2)` 的箭头挡住，把新箭头放到 `(4,2)` 根本不改变它的状态，
但判据仍然算了 1 分——生成器以为自己一直在制造依赖，其实每步都白送一个自由箭头。改成只统计
「当前还能飞」的箭头后，第 30 关的自由箭头从 23 降到 3 左右：

```python
if _distance_along(arrow, cell) is None:
    continue
if board.scan(arrow.x, arrow.y, arrow.direction).kind == "edge":   # 必须是自由箭头
    count += 1
```

**案例 2：多格箭头的合法判据错了，生成出「互相挡住」的死局（最严重的一次）**

把箭头从 1 格改成多格后，AI 给的落点判据是「从落点起连续空几格」，
于是出现了这样的代码路径：身体只占前几格、把「这几格空着」当成可以放。结果生成出来的关卡**无法通关**：

```
关卡 18 生成结果（贪心求解返回不可解）
  arrow#16 (4,9)->(4,7) UP   len=3  头前方: arrow (4,6) -> #17
  arrow#17 (4,6)->(2,6) LEFT len=3  头前方: arrow (1,6) -> #18
  arrow#18 (1,6)->(1,6) DOWN len=1  头前方: arrow (1,7) -> #19
  arrow#19 (1,7)->(1,7) RIGHT len=1 头前方: arrow (4,7) -> #16   ← 环！
  环: [[16, 19, 18, 17, 16]]
```

`#19` 放在 `(1,7)` 朝右时，射线尽头 `(4,7)` 属于更早放置的 `#16`，也就是说新箭头的头前方**被旧箭头堵死**，
按「谁挡着谁」的关系形成了 4 元环，这一关永远打不通。根因是我用「连续空格数」代替了
「一路空到棋盘边缘」：身体只占前面几格，射线尽头还有障碍物就会被漏判。修正后：

```python
def _ray_to_edge(board, cell, direction):
    """必须一路空到棋盘边缘；是则返回可用格数，否则返回 None。"""
    hit = board.scan(cell[0], cell[1], direction)
    return hit.distance if hit.kind == "edge" else None
```

修完再跑环检测：`环: 无`，30 关自动试玩全部通关。**这个错误不会让程序崩溃，只会让关卡变得不可通关**，
只有靠「求解器自检 + 环检测 + 逐关试玩」才抓得出来——这也是我把求解器写进生成流程的原因。

**案例 3：一条走廊起不来就终止整张棋盘，密度腰斩**

换成走廊构造后，第 30 关的箭头数反而从 34 掉到 **22.6 个**（目标 43）。诊断发现：
棋盘被前面的走廊切碎后，某个孤立空格四个方向都被堵着，从它起走廊一个箭头都放不下，
`_build` 就把「放不下了」当成结论提前收工。改成多试起点即可：

```python
# 修改后：先按「最长可用射线」排序，再在前几名里随机挑起点
candidates.sort(key=lambda item: item[0], reverse=True)
_, start, directions = rng.choice(candidates[:5])
```

修复后第 30 关回到 **44 个箭头 / 占格 44%**，而且走廊会优先从棋盘中间起笔，
不会全部挤在边上（原来的做法中间会留一大片空地）。

**案例 4：多个测试模块各自 `pygame.quit()`，整个进程 0xC0000005 崩溃**

加上 T01–T06 测试后，测试明明全过，但 `python -m unittest discover` 的汇总行不见了、退出码变成
`-1073741819`（0xC0000005，访问违例）。定位方法：分别单跑三个模块 → 都是 `exit=0`；
两两组合 → 只有含两个「建 App + `pygame.quit()`」模块的组合会崩。原因是 SDL 在解释器退出阶段
重复释放资源。修法是在测试模块的 `tearDownClass` 里不再调用 `pygame.quit()`，交给进程结束回收：

```python
# 注意：这里故意不调用 pygame.quit()。多个测试模块各自建 App、各自 quit 时，
# SDL 在解释器退出阶段会重复释放资源，实测会让整个进程以 0xC0000005 崩溃。
```

**案例 5：多格箭头方案「先修好、再回退」的取舍（外观 + 复杂度）**

一度想让箭头长得像参考图那样是一条彩色长条，于是把「一枚箭头 = 一串共线格子」做进了模型层，
并把渲染改成「箭身折线 + 顶端三角」。AI 给的画法是「每格画一个小方块再叠箭头图标」，
渲染出来是一节一节的“按键”，我改成箭身从**尾格外边缘**连到**头格中心**再补三角，
相邻箭头首尾接在同一条格边界上，走廊才连成一片（案例 2 的死局 Bug 也是在这个方案里修掉的）。

但改完实际试玩后发现两个问题：一是棋盘明显更挤更乱，一眼看不清「哪一枚箭头能点」；
二是模型和生成器都要为「身体」多写一层逻辑（共线约束、长度随机、渲染分段），复杂度和收益不成正比。
讨论后决定**回退成单格箭头**，只保留「走廊式生成」这个真正让谜题好玩的思路，并把界面重新做了一版
（深色渐变 + 柔光、霓虹箭头、胶囊信息条、进度条内嵌到面板、切界面淡入）：

| 回退前 | 回退后 |
| --- | --- |
| 箭头占 1~4 格连成长条，棋盘拥挤、难分辨 | 单格霓虹箭头，清爽、一眼能数清 |
| 模型 + 生成 + 渲染都要处理「身体」 | 模型只有「格子 + 方向」，规则与代码都最短 |
| 斜条纹背景 + 每格底块 + 元素堆叠 | 干净的渐变柔光底、无底块、信息分层清晰 |

这件事让我体会到：**AI 能把方案做出来，但「值不值得做」要自己判断**；能跑通不等于该留下。

**案例 6：中文字体缺字形与绘制顺序（细节）**

| 问题 | AI 原始做法 | 我的修改 |
| --- | --- | --- |
| 悬停提示被按钮压住 | 提示文字画在 y=670，底部按钮 y=686 且**后绘制**，文字被盖住一半 | 重排底部布局：进度与计数左右并排、提示做成带底衬的「药丸」并放在按钮之后绘制 |
| 提示符号显示成方块 | 用了 `✕`（U+2715），微软雅黑没有该字形 | 换成一定有的 `×`（U+00D7），方向符号按箭头方向动态取 `↑↓←→`（原来硬编码 `↑`，与「朝下」文案矛盾） |
| 墙与箭头格子分不清 | 墙就是一块少了箭头的同色方块 | 改成砖缝纹理 + 更深底色，一眼可辨 |

### 5.3 否决 / 调整过的 AI 建议

* **`pygame.sprite.Group` 管理箭头** → 否决。箭头最多 44 个，直接遍历 `board.arrows` 更直观，
  且 sprite 会把渲染状态与规则状态混进界面层，破坏分层。
* **`numpy` 合成音效** → 否决。为几秒音效引入大依赖不划算，改用标准库 `array` 拼 PCM。
* **预写 30 张手工关卡** → 否决。作业强调算法实现，程序化生成 + 可解性自检更能体现
  「路径检测、关卡状态管理」，而且关卡可无限重玩。
* **"点错直接失败" 的硬核规则** → 调整。改为三次机会，并保留「不会卡死」性质，对新手更友好。
* **箭头连成多格长条** → 做完又回退（见案例 5）。AI 能把方案实现出来，但「值不值得留下」由我判断。

---

## 六、总结与不足

**收获。**

1. 把「好玩」翻译成可判定的规则是这类小游戏最本质的一步：确定「能飞的条件＝正前方射线全空」之后，
   可解性证明、生成算法、提示算法全都顺理成章。
2. 生成器是最容易出错的地方，而且**错误不会崩、只会让关卡变得无聊或不可通关**——
   必须靠量化指标（占格率、可点箭头占比、求解器自检、环检测、逐关试玩）把它抓出来。
   本次三个生成器缺陷全都是这样发现的，其中案例 2 直接导致关卡不可通关。
3. AI 很适合写样板代码（绘制、控件、存档、测试模板），但**涉及算法正确性与边界情况必须自己验证**。
   本次六个真实缺陷里，四个是逻辑漏洞、两个是表现层问题，没有一个是「跑起来报错」能暴露的。
4. 界面的「好看」也是一样：AI 第一版给的斜条纹背景、每格底块、堆叠的元素，实际渲染出来很杂乱；
   重新梳理了配色层次、间距和留白（渐变柔光底、无底块、信息分组成胶囊、进度条收进面板）之后才顺眼。
   **能跑 ≠ 好看，好不好看必须自己盯着截图一帧帧调。**

**不足与可改进。**

1. 10×10 棋盘的占格率约 44%，还有提高空间；可以尝试「先随机密铺、再按拓扑序破环」的思路提高密度。
2. 关卡难度只由尺寸、箭头数、走廊长度控制，缺少「最短解长度 / 强制顺序深度」这类更精细的度量。
3. 没有背景音乐、没有移动端适配；动画只有位移、缩放与淡入，还没有粒子效果。

---

## 附录：文件与运行清单

```
homework/
├── README.md            项目说明（简介、环境、运行、操作、截图、算法、测试）
├── 实验报告.md          本文件
├── 博客.md              作业要求的博客稿（含 AIGC 过程表、测试表、PSP、心得）
├── 提交到GitHub.md      Git 与 GitHub 上传步骤、推荐 commit 信息
└── arrow_arrows/
    ├── main.py / __main__.py
    ├── core/{model,generator,solver}.py
    ├── ui/{app,board_view,scenes,widgets,shapes,theme,audio,storage}.py
    ├── tests/{test_core,test_ui_smoke,test_assignment_cases}.py
    └── tools/{screenshot.py,verify_levels.py,shots/*.png}
```

```bash
pip install pygame
python arrow_arrows/main.py                    # 开始游戏
python -m unittest discover -s arrow_arrows/tests -t .   # 38 个测试
python arrow_arrows/tools/verify_levels.py     # 30 关自动试玩报告
python arrow_arrows/tools/screenshot.py        # 无头渲染界面截图
```
